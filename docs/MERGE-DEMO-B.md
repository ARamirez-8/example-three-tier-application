# Merge Demo B

- **api** – Express.js REST API that handles task CRUD operations and communicates with the database.
- **db** – PostgreSQL database initialisation scripts and node-pg-migrate migration files.
- **infrastructure** – Terraform configuration for provisioning the application's GCP cloud resources.
- **web** – Nginx-based web tier that serves the frontend and proxies requests to the API.
